export enum PersonGroup {
{{#each groups}}
  {{sanitizedGroup}} = "{{group}}",
{{/each}}
}

export enum PersonRole {
{{#each roles}}
  {{sanitizedRole}} = "{{role}}",
{{/each}}
}

/*
export const PersonTaxonomy:Record<PersonGroup, Record<
  PersonRole,
  { role: string; description: string }>
> = {
{{#each grouped}}
  [PersonGroup.{{@key}}]: {
    {{#each this}}
      [PersonRole.{{sanitizedRole}}] : {
        description: "{{description}}",
        role: "{{role}}",
      },
    {{/each}}
  },
{{/each}}
};
*/
